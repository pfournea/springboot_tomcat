spring:
  profiles: tst,acc,prd
  cloud:
    config:
      uri: ${configServiceUrl}
      failFast: true
      username: ${configServiceUsername}
      password: ${configServicePassword}
      retry:
        maxAttempts: 5